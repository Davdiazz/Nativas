<?xml version="1.0" encoding="utf-8"?>
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Parte superior -->
    <item android:top="0dp" android:bottom="0sp">
        <shape android:shape="rectangle">
            <solid android:color="@color/Marfil_Oscuro" /> <!-- Color claro arriba -->
        </shape>
    </item>

    <!-- Parte inferior -->
    <item android:top="280sp">
        <shape android:shape="rectangle">
            <solid android:color="@color/Marfil" /> <!-- Color más oscuro abajo -->
        </shape>
    </item>
</layer-list>